// GCD: 最大公約数 / LCM: 最小公倍数
// lower_bound:「x以上」 / upper_bound:「x超過」
#include <bits/stdc++.h>
#include <atcoder/all>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
// --- 多倍長（Boost）: 超巨大整数が必要な時だけコメントアウトを解除 ---
// #include <boost/multiprecision/cpp_int.hpp>
// #include <boost/multiprecision/cpp_dec_float.hpp>
// using bigint = boost::multiprecision::cpp_int;
// using bigfloat = boost::multiprecision::cpp_dec_float_50;

using namespace std; using namespace atcoder;using namespace __gnu_pbds;
struct FastIO { FastIO() { cin.tie(nullptr); ios::sync_with_stdio(false); cout << fixed << setprecision(15); } } fast_io;

// --- 型定義 ---
using ll = long long; using ld = long double; using str = string; using ull = unsigned long long;
template<class T> using vc = vector<T>; template<class T> using vvc = vector<vector<T>>;
using pll = pair<ll, ll>; using tll = tuple<ll, ll, ll>;
using mint = modint998244353;
// using mint = modint1000000007;

// --- ループマクロ (rep, rrep の2種に統一・ステップ数対応) ---
#define OVERLOAD4(_1, _2, _3, _4, NAME, ...) NAME
#define rep1(i, n) for (ll i = 0; i < (ll)(n); i++)
#define rep2(i, l, r) for (ll i = (ll)(l); i < (ll)(r); i++)
#define rep3(i, l, r, s) for (ll i = (ll)(l); i < (ll)(r); i += (ll)(s))
#define rep(...) OVERLOAD4(__VA_ARGS__, rep3, rep2, rep1)(__VA_ARGS__)

#define rrep1(i, n) for (ll i = (ll)(n) - 1; i >= 0; i--)
#define rrep2(i, l, r) for (ll i = (ll)(r) - 1; i >= (ll)(l); i--)
#define rrep3(i, l, r, s) for (ll i = (ll)(r) - 1; i >= (ll)(l); i -= (ll)(s))
#define rrep(...) OVERLOAD4(__VA_ARGS__, rrep3, rrep2, rrep1)(__VA_ARGS__)

#define ALL(x) (x).begin(), (x).end()
#define rALL(x) (x).rbegin(), (x).rend()

// --- 入出力 (mint / vector対応) ---
template<int m> std::istream &operator>>(std::istream &is, static_modint<m> &v) { long long x; is >> x; v = x; return is; }
template<int m> std::ostream &operator<<(std::ostream &os, const static_modint<m> &v) { return os << v.val(); }
template<typename T> std::istream &operator>>(std::istream &is, vc<T> &v) { for (T &in : v) is >> in; return is; }
template<typename T> std::ostream &operator<<(std::ostream &os, const vc<T> &v) { rep(i, v.size()) os << v[i] << (i + 1 == (ll)v.size() ? "" : " "); return os; }
inline void scan() {}
template<class Head, class... Tail> inline void scan(Head& head, Tail&... tail) { std::cin >> head; scan(tail...); }
#define LL(...) ll __VA_ARGS__; scan(__VA_ARGS__)
#define STR(...) str __VA_ARGS__; scan(__VA_ARGS__)

// --- 型変換 ---
inline int si(char c) { return c - '0'; } inline int si(const str& s) { return stoi(s); }
inline ll cll(char c) { return c - '0'; } inline ll cll(const str& s) { return stoll(s); }
inline char sc(ll n) { return '0' + n; } inline char sc(const str& s) { return s[0]; }
inline str ss(ll n) { return to_string(n); } inline str ss(char c) { return str(1, c); }

// --- 便利マクロ・関数 ---
#define Yes cout << "Yes\n"
#define No cout << "No\n"
#define pb push_back
#define pf() pop_front()
#define CJ cout_join
#define mp make_pair
#define fir first
#define sec second

template<class T> using priority_max = priority_queue<T>;
template<class T> using priority_min = priority_queue<T, vector<T>, greater<T>>;

template<class T, class U> inline bool chmax(T& a, const U& b) { if (a < b) { a = b; return 1; } return 0; }
template<class T, class U> inline bool chmin(T& a, const U& b) { if (a > b) { a = b; return 1; } return 0; }

template<typename T> void cout_join(const vector<T>& v, string sep = "") { rep(i, v.size()) cout << v[i] << (i + 1 == (ll)v.size() ? "" : sep); cout << "\n"; }
void print() { cout << "\n"; } template<class T, class... Args> void print(const T& a, const Args&... args) { cout << a << (sizeof...(args) ? " " : ""); print(args...); }

template<class C, class T> ll lower_idx(const C& c, const T& x) { return lower_bound(c.begin(), c.end(), x) - c.begin(); }
template<class C, class T> ll upper_idx(const C& c, const T& x) { return upper_bound(c.begin(), c.end(), x) - c.begin(); }
template<class C, class T> ll lower_idx0(const C& v, const T& x) { return lower_bound(v.begin(), v.end(), x, [](const auto& a, const auto& val) { return a[0] < val; }) - v.begin(); }
template<class C, class T> ll upper_idx0(const C& v, const T& x) { return upper_bound(v.begin(), v.end(), x, [](const auto& val, const auto& a) { return val < a[0]; }) - v.begin(); }

template<class T, class U> auto ifel(bool cond, T t, U f) { return cond ? t : f; }
void YES(bool cond) { print(cond ? "Yes" : "No"); }

template<typename T> T MAX(const vector<T>& v) { return *max_element(v.begin(), v.end()); }
template<class T, class... Args> auto MAX(T a, Args... args) { if constexpr (sizeof...(args) == 0) return a; else { auto b = MAX(args...); return (a > b ? a : b); } }
template<typename T> T MIN(const vector<T>& v) { return *min_element(v.begin(), v.end()); }
template<class T, class... Args> auto MIN(T a, Args... args) { if constexpr (sizeof...(args) == 0) return a; else { auto b = MIN(args...); return (a < b ? a : b); } }
template<typename T> T SUM(const vector<T>& v) { T s = 0; for (const auto& x : v) s += x; return s; }
template<class... Args> auto SUM(Args... args) { return (args + ...); }

template<typename T> typename T::value_type& py_at(T& v, long long i) { long long n = v.size(); if (n == 0 || i < -n || i >= n) { std::cerr << "[py_at Error] Out of range! size: " << n << ", index: " << i << std::endl; return v.at(i); } return v[(i % n + n) % n]; }
template<typename T> const typename T::value_type& py_at(const T& v, long long i) { long long n = v.size(); if (n == 0 || i < -n || i >= n) { std::cerr << "[py_at Error] Out of range! size: " << n << ", index: " << i << std::endl; return v.at(i); } return v[(i % n + n) % n]; }

constexpr bool bit(long long i, long long j) { return (i >> j) & 1; }

template<class T> T vvvc(T v) { return v; }
template<class T, class... A> auto vvvc(ll n, T v, A... a) { return vector(n, vvvc(v, a...)); }
template<class S> void mkuni(S& v) { sort(v.begin(), v.end()); v.erase(unique(v.begin(), v.end()), v.end()); }

constexpr ll ipow(ll b, ll e) { ll r = 1; while (e > 0) { if (e & 1) r = (ll)((__int128)r * b); b = (ll)((__int128)b * b); e >>= 1; } return r; }
constexpr ll modpow(ll b, ll e, ll m) { b %= m; if (b < 0) b += m; ll r = 1; while (e > 0) { if (e & 1) r = (__int128)r * b % m; b = (__int128)b * b % m; e >>= 1; } return r; }
constexpr ll modinv(ll b, ll m) { return modpow(b, m - 2, m); }

template<typename T> T GCD(const vector<T>& v) { T r = 0; for (auto x : v) r = std::gcd(r, x); return r; }
template<class T, class... Args> auto GCD(T a, Args... args) { if constexpr (sizeof...(args) == 0) return a; else return std::gcd(a, GCD(args...)); }
template<typename T> T LCM(const vector<T>& v) { T r = 1; for (auto x : v) r = std::lcm(r, x); return r; }
template<class T, class... Args> auto LCM(T a, Args... args) { if constexpr (sizeof...(args) == 0) return a; else return std::lcm(a, LCM(args...)); }
template <class T>
struct SortedList {
    tree<pair<T, int>, null_type, less<pair<T, int>>, rb_tree_tag, tree_order_statistics_node_update> st;
    int id = 0;

    void insert(T x) { st.insert({x, id++}); }
    void erase(T x) {
        auto it = st.lower_bound({x, 0});
        if (it != st.end() && it->first == x) st.erase(it);
    }
    T operator[](int k) const { return st.find_by_order(k)->first; }
    int index(T x) const { return st.order_of_key({x, 0}); }
    int size() const { return st.size(); }
};
template <class T>
struct TreapVector {
    struct Node {
        T val; int pri, cnt; bool rev; Node *l, *r;
        Node(T v) : val(v), pri(rand()), cnt(1), rev(0), l(0), r(0) {}
    } *root = 0;

    int cnt(Node* t) { return t ? t->cnt : 0; }
    void upd(Node* t) { if (t) t->cnt = 1 + cnt(t->l) + cnt(t->r); }
    void push(Node* t) {
        if (t && t->rev) {
            swap(t->l, t->r);
            if (t->l) t->l->rev ^= 1;
            if (t->r) t->r->rev ^= 1;
            t->rev = 0;
        }
    }
    void split(Node* t, int k, Node*& l, Node*& r) {
        if (!t) return void(l = r = 0);
        push(t);
        if (k <= cnt(t->l)) split(t->l, k, l, t->l), r = t;
        else split(t->r, k - cnt(t->l) - 1, t->r, r), l = t;
        upd(t);
    }
    void merge(Node*& t, Node* l, Node* r) {
        push(l); push(r);
        if (!l || !r) t = l ? l : r;
        else if (l->pri > r->pri) merge(l->r, l->r, r), t = l;
        else merge(r->l, l, r->l), t = r;
        upd(t);
    }

    void insert(int k, T val) { Node *l, *r; split(root, k, l, r); merge(l, l, new Node(val)); merge(root, l, r); }
    void erase(int k) { Node *l, *m, *r; split(root, k + 1, l, r); split(l, k, l, m); merge(root, l, r); delete m; }
    void reverse(int L, int R) {
        if (L >= R) return;
        Node *a, *b, *c; split(root, R + 1, b, c); split(b, L, a, b);
        if (b) b->rev ^= 1;
        merge(b, a, b); merge(root, b, c);
    }
    T operator[](int k) {
        for (Node* t = root; t; ) {
            push(t); int c = cnt(t->l);
            if (k == c) return t->val;
            t = (k < c) ? t->l : (k -= c + 1, t->r);
        }
        return T();
    }
    int size() { return cnt(root); }
};
// --- デバッグ ---
#ifdef LOCAL
#define dmp(...) dmpr(cerr, __LINE__, ##__VA_ARGS__)
#else
#define dmp(...) void(0)
#endif
template<class t, class u> ostream& operator<<(ostream& os, const pair<t, u>& p) { return os << "{" << p.first << "," << p.second << "}"; }
void dmpr(ostream& os, ll l) { os << endl; }
template<class T, class... A> void dmpr(ostream& os, ll l, const T& t, const A&... a) { if (l) os << "[L" << l << "] "; os << t << " "; dmpr(os, 0, a...); }

// --- 定数 ---
const vc<ll> dx = {1,0,-1,0,1,-1,-1,1}; const vc<ll> dy = {0,1,0,-1,1,-1,1,-1};
const double pi = 3.14159265358979323846264338;
constexpr ll INF = 2e18;

int main() {
  
  
  
  
  
  
  
  
  
  
}
